name: Multi Job Workflow
on: push
jobs:
  backend:
    name: Backend Build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [14, 16]
    steps:
      - uses: actions/checkout@v2
  frontend:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        browser: [chrome, firefox]
